@php $mensaje = $mensaje ?? null; @endphp
<label><span class="field-label">Chat</span><select class="input-sm" name="chat_id" required>@foreach($allChats as $chat)<option value="{{ $chat->id }}" @selected($mensaje && (int)$mensaje->chat_id === (int)$chat->id)>#{{ $chat->id }} {{ $chat->nombre }}</option>@endforeach</select></label>
<label><span class="field-label">Emisor</span><select class="input-sm" name="emisor_id" required>@foreach($allUsers as $usuario)<option value="{{ $usuario->id }}" @selected($mensaje && (int)$mensaje->emisor_id === (int)$usuario->id)>{{ $usuario->name }}</option>@endforeach</select></label>
<label><span class="field-label">Receptor</span><select class="input-sm" name="receptor_id" required>@foreach($allUsers as $usuario)<option value="{{ $usuario->id }}" @selected($mensaje && (int)$mensaje->receptor_id === (int)$usuario->id)>{{ $usuario->name }}</option>@endforeach</select></label>
<label><span class="field-label">Editado</span><input type="hidden" name="editado" value="0"><select class="input-sm" name="editado"><option value="0" @selected(!$mensaje || !$mensaje->editado)>No</option><option value="1" @selected($mensaje && $mensaje->editado)>Sí</option></select></label>
<label class="full"><span class="field-label">Contenido</span><textarea class="input-sm" name="contenido" rows="3" required>{{ old('contenido', $mensaje->contenido ?? '') }}</textarea></label>
<label><span class="field-label">Leído en</span><input class="input-sm" type="datetime-local" name="read_at" value="{{ old('read_at', $mensaje && $mensaje->read_at ? $mensaje->read_at->format('Y-m-d\TH:i') : '') }}"></label>
