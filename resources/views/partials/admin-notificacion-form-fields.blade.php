@php $notificacion = $notificacion ?? null; @endphp
<label><span class="field-label">Usuario</span><select class="input-sm" name="user_id" required>@foreach($allUsers as $usuario)<option value="{{ $usuario->id }}" @selected($notificacion && (int)$notificacion->user_id === (int)$usuario->id)>{{ $usuario->name }}</option>@endforeach</select></label>
<label><span class="field-label">Tipo</span><select class="input-sm" name="tipo" required>@foreach(['apuesta','promo','alerta','chat','info','mensaje','sistema'] as $tipo)<option value="{{ $tipo }}" @selected(old('tipo', $notificacion->tipo ?? 'info') === $tipo)>{{ ucfirst($tipo) }}</option>@endforeach</select></label>
<label class="full"><span class="field-label">Título</span><input class="input-sm" name="titulo" value="{{ old('titulo', $notificacion->titulo ?? '') }}" required></label>
<label class="full"><span class="field-label">Mensaje</span><textarea class="input-sm" name="mensaje" rows="3">{{ old('mensaje', $notificacion->mensaje ?? '') }}</textarea></label>
<label><span class="field-label">Fecha</span><input class="input-sm" type="datetime-local" name="fecha" value="{{ old('fecha', $notificacion && $notificacion->fecha ? \Illuminate\Support\Carbon::parse($notificacion->fecha)->format('Y-m-d\TH:i') : now()->format('Y-m-d\TH:i')) }}" required></label>
<label><span class="field-label">Leída</span><input type="hidden" name="leido" value="0"><select class="input-sm" name="leido"><option value="0" @selected(!$notificacion || !$notificacion->leido)>No</option><option value="1" @selected($notificacion && $notificacion->leido)>Sí</option></select></label>
