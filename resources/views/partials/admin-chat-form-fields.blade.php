@php $chat = $chat ?? null; @endphp
<label class="full"><span class="field-label">Nombre</span><input class="input-sm" name="nombre" value="{{ old('nombre', $chat->nombre ?? '') }}" placeholder="Chat privado"></label>
<label><span class="field-label">Creador</span><select class="input-sm" name="user_id" required>@foreach($allUsers as $usuario)<option value="{{ $usuario->id }}" @selected($chat && (int)$chat->user_id === (int)$usuario->id)>{{ $usuario->name }}</option>@endforeach</select></label>
<label><span class="field-label">Participante 1</span><select class="input-sm" name="user_one_id">@foreach($allUsers as $usuario)<option value="{{ $usuario->id }}" @selected($chat && (int)$chat->user_one_id === (int)$usuario->id)>{{ $usuario->name }}</option>@endforeach</select></label>
<label><span class="field-label">Participante 2</span><select class="input-sm" name="user_two_id">@foreach($allUsers as $usuario)<option value="{{ $usuario->id }}" @selected($chat && (int)$chat->user_two_id === (int)$usuario->id)>{{ $usuario->name }}</option>@endforeach</select></label>
<label><span class="field-label">Activo</span><input type="hidden" name="activo" value="0"><select class="input-sm" name="activo"><option value="1" @selected(!$chat || $chat->activo)>Sí</option><option value="0" @selected($chat && !$chat->activo)>No</option></select></label>
<label><span class="field-label">Último mensaje</span><input class="input-sm" type="datetime-local" name="last_message_at" value="{{ old('last_message_at', $chat && $chat->last_message_at ? $chat->last_message_at->format('Y-m-d\TH:i') : '') }}"></label>
